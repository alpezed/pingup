/** @type {import('prettier').Config} */
const config = {
  singleQuote: false,
  jsxSingleQuote: false,
  trailingComma: "all",
  useTabs: false,
  tabWidth: 2,
  semi: true,
  arrowParens: "always",
  bracketSpacing: true,
  quoteProps: "consistent",
  printWidth: 80,
};

export default config;
